<div id="homepage-main" class="container shadow-lg p-3 mb-5 bg-body rounded" >
  <ol class="list-group">
    {{#each posts as |post|}}
    <li class="list-group-item shadow-lg p-3 mb-5 bg-body rounded">
      {{> post-info post }}
    </li>
    {{/each}}
  </ol>
</div>